Place icon files here for electron-builder:

- `icon.ico`  — Windows (256×256 recommended, multi-size .ico)
- `icon.icns` — macOS (.icns bundle)
- `icon.png`  — Linux (512×512 PNG)

electron-builder reads these automatically based on the win/mac/linux icon paths in package.json.
