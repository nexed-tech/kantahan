services:
  kantahan:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ${MEDIA_PATH:-/media}:/media/karaoke
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/karaoke.db
    restart: unless-stopped
